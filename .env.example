# Server-side only — never exposed to the client. The proxy routes and the
# capture script read it. URL includes the provider key (e.g. Helius).
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY
